# Auth0 tenant verified in DECISIONS.md — Authorization Code + PKCE, access
# token audience is the resource server API, not the ID token.
VITE_AUTH0_DOMAIN=dev-yg.us.auth0.com
VITE_AUTH0_AUDIENCE=https://bbl-candidate-test-api

VITE_AUTH0_CLIENT_ID=H9F6QG5SzTKMv0tbmgxLj9LjG1EKVllA

# Backend API this SPA calls with the access token attached. Backend runs on
# 3001 because the frontend itself has to own 3000 (fixed Auth0 callback URL).
VITE_API_BASE_URL=http://localhost:3001
